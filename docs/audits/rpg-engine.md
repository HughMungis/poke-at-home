- **Lines 1042–1048, 1077–1103 — Concurrent rebirths award multiple rewards for one completed life.** Two `!rebirth confirm` calls can both read the same level-25 player and pass the level check before either writes. Their write transactions then run sequentially, but neither rechecks eligibility: both reset the player and increment `rebirths` and `legacy_slots`. The player receives two permanent slots despite reaching level 25 only once.

- **Lines 1019–1029 — Concurrent legacy purchases can spend the same slot twice.** Entering `with _db()` does not begin a SQLite transaction for the subsequent `SELECT`s under this connection configuration. With one free legacy slot and bag rank 0, two `!legacy bag confirm` calls can both pass line 1021 before either writes. Both then increment the counters and expand the bag, granting two ranks and six bag spaces for one earned slot. The later writes serialize, but the eligibility checks do not.

- **Lines 1064–1068, 1080–1088 — Rebirth resurrects supposedly permanent trophies.** The keep-item lookup accepts existing `legacy=1` gear without checking its legacy status. A player can surrender weapon ID 42 during one rebirth, reach level 25 again, and issue `!rebirth confirm 42`. The engine changes that permanent, never-equippable trophy into an equipped memento, restoring its combat contribution contrary to lines 746–748 and 895–897.

- **Lines 1147–1151, 1169–1173, 1191–1196 — Concurrent class changes can overdraw gold and double-charge a promotion.** A level-6 mage with exactly 250 gold sends two `!class firemage` commands. Both read the original mage and balance before either update, so both pass the checks and subtract 250. The final balance is **−250 gold**, with 500 recorded as spent, although only one class promotion occurred.

- **Lines 1059–1066 — Crafted item IDs crash rebirth handling.** For an eligible level-25 player, `!rebirth confirm 9223372036854775808` passes `isdigit()` and `int()`, but SQLite cannot bind that integer because it exceeds its signed 64-bit range, raising an uncaught `OverflowError`. `!rebirth confirm ²` also passes `isdigit()` but raises `ValueError` in `int(t)`. Neither input reaches the intended invalid-item response.

- **Lines 1135–1157 — Tier-3 promotion bypasses the documented class-line lock.** A level-12 warrior with 250 gold and `mastered:firemage` from a previous life can issue `!class pyroclast`. This branch checks mastery, level, and gold, but never the current class line, and returns before the line-lock logic. The player switches from the warrior path to the mage path without rebirth, contradicting the stated restriction at lines 1181–1185.
