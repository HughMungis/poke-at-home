- **Lines 668–673, 635–637, 848–858 — A blocked ffmpeg pipe can wedge the broadcast and prevent shutdown saves.** If ffmpeg stops consuming stdin at hour nine—for example, its process is stopped with `SIGSTOP`—the pipe fills and `proc.stdin.write()` blocks indefinitely. SIGTERM only sets `stop["now"]`; it does not unblock the write. The main loop never reaches `save_progress()` or the subprocess timeout, and the live-stream interlock cannot shut down the encoder either.

- **Lines 417–428, 554–564 — An initial overlay-rendering failure stalls the entire video pipeline indefinitely.** With the default live overlay enabled, remove `/usr/share/fonts/truetype/nanum/NanumBarunGothicBold.ttf`. Every `overlay_png()` call raises, the exception is silently swallowed, and `last_png` remains `None`. The feeder keeps the FIFO open without sending any PNG. ffmpeg waits for its overlay input, eventually stops consuming game frames, and blocks the main thread’s frame write. There is no fallback image or timeout.

- **Lines 794–817, 900–929 — Failed progress saves do not prevent the destructive stuck reset.** `save_progress()` catches its own write failures and returns no success indicator, so the archive branch cannot tell whether it saved the current run. For example, the last checkpoint has zero badges, the in-memory run has just earned one, and writes to the three temporary save paths fail while existing checkpoints remain readable and the archive directory remains writable. The code copies the old zero-badge files, announces success, and calls `env.reset()`, permanently discarding the newly earned badge despite the explicit promise to refuse reset when the safety net fails.

- **Lines 900–926, 951–957 — Checkpoints can combine different generations of emulator state and exploration data.** The three replacements are individually atomic but have no shared commit point. Suppose the existing checkpoint contains three badges and 3,843 explored tiles; after a fresh-run reset, saving replaces `RUN_STATE` with the starting state, then the process is killed before replacing the map and JSON. Restart loads the starting character alongside the previous season’s exploration map and coordinates. The policy receives mismatched inputs, and the exploration counter retains progress belonging to a different run.

- **Lines 951–983, 724–727 — Failed resume leaves partially loaded state active instead of starting fresh.** Given a valid three-badge `run_state.state` and a truncated `run_explore_map.npy`, loading the emulator succeeds and `np.load()` raises. The handler reports “starting fresh” and returns `None`, but never resets the emulator or rolls back the changes. `main()` consequently retains the observation from `init.state` while running the three-badge emulator with freshly reset exploration bookkeeping. A later successful save overwrites the persisted exploration history with that incomplete state.
